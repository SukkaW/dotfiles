aptupdate () {
  echo -n "
=====================
sudo apt update
---------------------
"
  sudo apt update
  echo -n "
---------------------
sudo apt list --upgradable
---------------------
"
  sudo apt list --upgradable
  echo "====================="
}

aptupgrade () {
  sudo apt upgrade -y
}
