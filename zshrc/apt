aptupd () {
  echo "====================="
  echo "sudo apt update"
  echo "---------------------"
  sudo apt update
  echo "---------------------"
  echo "sudo apt list --upgradable"
  echo "---------------------"
  sudo apt list --upgradable
  echo "====================="
}

aptupg () {
  sudo apt upgrade -y
}
